class CreateRequest:

    def __init__(self, username, first_name, last_name, primary_loc, like_user):
        self.username = username
        self.first_name = first_name
        self.last_name = last_name
        self.primary_loc = primary_loc
        self.like_user = like_user
